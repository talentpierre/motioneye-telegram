##config file
##telegram
echo "Config einlesen"

var_apiKey=
var_chatID=

##ping devices
var_pingDeviceArray=()

var_scriptDir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
var_logfile="$var_scriptDir/motion-send.log"
var_metConf="$var_scriptDir/met.conf"